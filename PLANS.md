# Capybara 自治执行计划

本文件是 `CODEX_AUTONOMOUS_GAME_PRODUCTION_GOAL_v2.md` 的可执行计划视图。目标契约定义最终范围和不可降低的质量标准；本文件只记录当前 Stage、证据标准、任务顺序和恢复入口。跨会话事实状态统一写入 `docs/AUTONOMOUS_STATUS.md`。

## 当前基线

- 当前 Stage：`3 — 高品质视觉原型与美术流水线`。
- 当前开发分支：`codex/autonomous-v1`。
- 最近完成的 Stage 0.5 实现 commit：`481a140 chore(governance): establish autonomous recovery and CI`。
- 最近稳定标签：`capybara-stage-02`（Stage 2 合并后创建）。
- Stage 0 当前证据：`tools/check_project.ps1` 退出码 `0`，自动测试 `17/17`，主场景烟雾退出码 `0`，暂停/焦点/退出窗口复核通过。
- 远程同步：2026-09-16已验证Git凭据与GitHub集成，仓库为public。因GIT-002旧隔离图像历史，仅发布独立当前tree审阅快照；原开发分支与标签不得直接推送。

## 永久执行循环

1. 恢复上下文：读取目标契约、`AGENTS.md`、本文件、自治状态、最近提交和当前测试。
2. 选择当前 Stage 中最高价值的可验证小批次。
3. 在改动前写明验收证据，避免用实现意图代替结果。
4. 实现一个紧密相关批次，不同时重写无关架构。
5. 运行统一检查、相关专项测试、烟雾测试和必要视觉检查。
6. 自审并登记问题；Blocker/Critical/High 不得跨 Stage 合并。
7. 更新状态、backlog、变更记录和决策。
8. 原子 commit；Stage 通过后合并、tag、安全 push，并自动继续。

## Stage 0.5 当前任务

### 目标

让一次完全中断后的新会话仅凭仓库文件即可确定当前 Stage、最后稳定点、测试证据、未解决问题、待同步提交和下一条命令。

### 验收标准

- 目标契约在根目录并被 `AGENTS.md` 明确引用为 Stage 0 后最高优先级指令。
- `PLANS.md`、自治状态、质量门、已知问题、发布准备和恢复说明齐全且互相引用。
- 旧路线图、技术设计、美术规范、backlog 和决策日志不再与大型开放世界/高品质美术/自治 Git 规则冲突。
- `tools/check_project.ps1` 生成统一文本日志和 JSON 摘要，验证治理文件、Godot 版本、导入、测试、主场景烟雾和 Git 空白错误。
- CI 固定 Godot `4.7.2-stable`，在 Windows runner 执行统一检查并保存日志。
- Stage 0.5 检查和自审通过，建立原子 commit；远程不可安全确认时记录待同步状态。

### 执行清单

- [x] 重新验证 Stage 0 并建立 `803b90d` / `capybara-stage-00` 稳定点。
- [x] 创建并切换 `codex/autonomous-v1`。
- [x] 在 `AGENTS.md` 建立目标契约优先级和恢复顺序。
- [x] 创建全部 Stage 0.5 治理和恢复文档。
- [x] 统一检查入口、结构化结果、失败路径、超时、并行日志和治理自检。
- [x] 建立固定 SHA256 的 Godot 4.7.2 Windows CI、导出模板和导出/启动烟雾。
- [x] 更新旧入口、路线图、技术设计、美术规范、backlog、决策和变更记录。
- [x] 完成本地强制质量门与独立代码/QA 审查；High 发现已修复。
- [x] 建立 Stage 0.5 原子恢复点；远程认证失效按 `GIT-001` 记录待同步。

## 已完成 Stage：Stage 1

Stage 0.5 通过后自动开始，不等待常规设计批准。

第一批优先顺序：

- [x] 建立可测试的玩家状态机与 8 方向 facing 数据。
- [x] 建立 typed `InteractionContext`、`InteractionResult` 和 Interactable 协议。
- [x] 实现稳定的交互候选排序、玩家传感器与 prompt 入口。
- [x] 添加拾取物、资源点、床、箱子、门灰盒实例。
- [x] 添加输入设备识别和键鼠/手柄提示切换。
- [x] 添加场景出生点、转场和重复玩家防护。
- [x] 添加 F3 调试覆盖层和本地化运行时诊断。
- [x] 扩展单元/集成测试并完成 20 分钟灰盒 soak。

Stage 1 退出标准以目标契约第 13 节和 `docs/QUALITY_GATES.md` 为准。

## 已通过质量门：Stage 2

Stage 1 通过后已自动开始，不等待常规设计批准。

优先顺序：

- [x] 建立 typed `ItemDefinition`、稳定 ID 和事务型 `ContentRegistry`。
- [x] 实现 24 格背包、堆叠、容量、溢出、拆分和排序。
- [x] 完成背包玩家操作、快捷栏装备、储物箱和上下文工具消费链。
- [x] 完成设置、四分辨率×三缩放显示矩阵和辅助功能消费者。
- [x] 扩展 `zh_CN` / `en` 本地化框架与缺 key 检查。
- [x] 完成 Save v1 正式玩家入口、恢复提示和语义 backup 回退；暂停菜单窗口与损坏回归通过。
- [x] 运行随机库存、存档恢复/迁移、集成与 Windows 构建质量门。

Stage 2 退出标准以目标契约第 13 节和 `docs/QUALITY_GATES.md` 为准。

最终证据：exact clean `c7f91fe` run `20260903T041100794Z-p23616-cf38b6eb` 为 616/616；独立代码/QA 终审均为 Blocker/Critical/High 0/0/0。本地 merge/tag 完成后自动进入 Stage 3。

## 当前 Stage：Stage 3

Stage 2 已合并为本地 `main` commit `25efe67` 并创建 `capybara-stage-02`。Stage 3 不把灰盒或未经修整的 AI 输出当成正式资产。

优先顺序：

- [ ] 生成 6–12 个高差异原创主角候选，创建联系表、实际尺寸预览、manifest 和独立视觉审查。
  - [x] 发现旧 C1/D1 派生链使用未核验会话照片后，将整组图片、联系表与旧审查记录隔离，禁止复用。
  - [x] 仅用已登记 CC0 解剖参考或原创文字生成 U1/V1/W1/Z1/AA1/AD1 六个逐文件登记的视觉概念。
  - [x] 创建带 output/input SHA-256 映射的清洁链联系表；独立视觉审查无 Blocker/Critical/High。
  - [x] 仅用原创文字生成 AG1/AH1；独立审查选 AG1 为长低体态与宽钝口鼻解剖锚点，AH1 因 144 px 物种漂移拒绝。
  - [ ] 依据 AG1 的文字化规则重绘 down-right 透明技术候选；U2/AA2/V2/AG2 的长腿、光晕、宠物眼或假透明失败均已登记，不再盲抽同类提示词。
- [ ] 锁定 `PLAYER_MASTER_V1` 并制作四方向一致性样板。
- [ ] 建立家园视觉测试场景和高品质地面、水岸、植物、房屋、家具样板。
  - [x] 完成并锁定文字-only 家园 A1 视觉方向：用户确认精美；水作为空间骨架，房屋—水车—码头形成地标三角；两条独立审查均无 Blocker/Critical/High。
  - [x] 从 A1 提取独立可玩功能 blockout：64px 网格、约 144px 角色、水陆碰撞、主/次路、桥、主门、菜地、水车、码头与九个稳定站位；不把整张插画当地图。
  - [x] 在 blockout 上建立统一环境 StyleProfile、世界坐标水粉地表、动态水和双层岸线技术样板。
  - [x] 2026-09-12：独立环境预览复用暂停与设置，隐藏存读档并仅在本次运行应用设置；显式补齐手柄 UI 映射，真实事件及 GPU 窗口验证见 `docs/ENVIRONMENT_PREVIEW.md`。
  - [ ] 以模块化高品质房屋、桥、菜地、水车、码头和植物逐步替换几何地标。
    - [x] 2026-09-12：独立木桥替换平面矩形桥，复用绘本木纹，双向完整过桥与栏杆真实碰撞通过；五视角确认前后栏杆遮挡，仍属当前固定尺寸技术模块。
    - [x] 2026-09-12：水车替换静态圆轮，独立轮子/支架/进出水表现已接入；减少动态、停水、操作站位碰撞与南北排序通过原型检查，水生态模拟仍待Stage5。
    - [x] 2026-09-12：独立码头替换平面矩形；384×192平台伸入河面，192px岸侧入口及四侧护栏真实移动检查，水体碰撞只扣除桥面覆盖区；五视角GPU与独立审查见DOCK_REVIEW.md。
    - [x] 2026-09-12：一次原创阔叶材质生成，原生有机树冠、三种轮廓UV变体、树干碰撞/排序和独立阴影替换五处圆形占位；实际GPU Alpha与独立实景复审通过原型用途。ENV3-002保留正式枝叶层次差距。
    - [x] 主屋 A1 视觉概念建立并逐文件登记；造型/材质可用于分层重建，但生成与一次背景提取均为烘焙棋盘 RGB。
    - [ ] 依据主屋 A1 做可控分层透明重建，通过四底边缘、独立屋顶/墙体、游戏尺寸和实际场景检查后再替换几何房屋。
      - [x] 2026-09-12：暂停稿三层 SVG 技术样件已验证透明、四底、实际门位/墙体碰撞和屋顶遮挡；仍需恢复 A1 绘本材质后才能批准正式房屋。
      - [x] 2026-09-12：一次生成三分区灰泥/木纹/屋瓦材质板，独立 SVG 表面分区与分面 UV 已接入；实际 GPU 对照有可见改善、65,536 个 Alpha 样本无变化；仍保留右坡瓦片尺度与细节密度统一事项。
- [ ] 建立 `UI_MASTER_V1` 与非默认组件样板。
- [ ] 原型化动态水面、左上光、接触阴影、前景遮挡和减少动态兼容。
  - [x] 建立世界坐标绘本水面 shader；真实 Compatibility GPU 渲染通过，减少动态冻结水纹，高对比增强分离，并由自动测试覆盖。
  - [x] 固定 35° 相机、左上光、下右接触阴影、64px 网格、144px 玩家与家园色板数据；加入独立前景树冠淡化并验证真实玩家进入/离开。
  - [x] 增加 18px 浅水岸线带、8px 内侧高光和 12 个确定性低密度微粒；减少动态冻结微粒时间。
  - [ ] 增加局部岸边泡沫、更细致的模块接触阴影与四种时段/天气参数。
- [ ] 产出上午、黄昏、雨天、夜晚固定截图并完成三轮审查。
- [ ] 比较 2D cutout 与 Blender 正交预渲染角色动画样板并记录选择。
  - [x] 建立分层原创 SVG cutout 技术原型，并通过 Godot 透明渲染、四底边缘和角色本体 144 px 检查。
  - [ ] 用 AG1 的长低体态重构 cutout v002，并在 144 px 实景验证头身比例、小圆耳、低位围巾与有厚度的叶包；失败草样不晋级。
  - [ ] Blender 本机缺失且官方便携 ZIP 下载被安全层拒绝；获得明确安装授权或工具可用后再完成正交预渲染对比。
- [ ] 完善生成、联系表、PNG/alpha、图集和 Godot 导入自动化。
  - [x] 建立零第三方依赖的 PNG/Alpha/命名/重复哈希/manifest 验证与联系表生成，并接入统一门禁。
  - [ ] 在透明候选阶段补齐四底 Alpha 彩边预览；PLAYER_MASTER_V1 后继续完成图集裁切、pivot、Godot 导入预设与四方向一致性检查。

Stage 3 退出标准以目标契约第 13 节与 `docs/QUALITY_GATES.md` 为准；视觉必须明显达到高分辨率非像素绘本样板，而不是更精致的灰盒。

## 后续 Stage 索引

| Stage | 主题 | 稳定证据核心 |
|---:|---|---|
| 2 | 数据、背包、设置、本地化、可靠存档 | 随机库存回归、备份/损坏恢复、迁移测试 |
| 3 | 高品质视觉原型与美术流水线 | 四套固定截图、角色方案 A/B、资产证据链 |
| 4 | 大型世界、分块流送、程序化外围 | 60 分钟探索、确定性、重载一致性、内存证据 |
| 5 | 地形、水生态、建造核心 | 200 次编辑、差量保存、生态和 NPC 响应 |
| 6 | 农业、制作、烹饪、经济、住宅 | 完整生产循环与手柄流程 |
| 7 | 钓鱼、采集、划船、拍照、收藏 | 活动差异化、条件表、辅助难度 |
| 8 | NPC、关系、任务、社区事件 | 多地区故事弧、死锁防护、建设响应 |
| 9 | 8 个正式地区与程序化远野内容 | 主线完整、内容下限、无正式灰盒 |
| 10 | 动画、VFX、音频、UI、叙事精修 | 全场景截图审查与正式资产替换 |
| 11 | 平衡、性能、可靠性、无障碍、本地化 | 2 小时 soak、性能预算、零阻断问题 |
| 12 | Windows RC 与 Steam 就绪 | 可复现构建、干净机验证、权利清单、RC tag |

## 恢复入口

```powershell
Set-Location -LiteralPath 'E:\Capybara'
git status --short --branch
git log -20 --oneline --decorate
Get-Content -LiteralPath 'CODEX_AUTONOMOUS_GAME_PRODUCTION_GOAL_v2.md' -Raw
Get-Content -LiteralPath 'AGENTS.md' -Raw
Get-Content -LiteralPath 'PLANS.md' -Raw
Get-Content -LiteralPath 'docs\AUTONOMOUS_STATUS.md' -Raw
pwsh -NoProfile -File .\tools\check_project.ps1
```

不得只凭聊天摘要恢复；以上仓库证据优先。
